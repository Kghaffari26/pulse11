"use client";

// Project-scoped task list — full implementation (completion toggle,
// detail panel) lands in VYBE-401/403. Placeholder keeps the project
// detail tabs compiling.
export function ProjectTasksPanel({ projectId: _projectId }: { projectId: string }) {
  return (
    <div className="rounded-md border p-6 text-sm text-muted-foreground">
      Task list coming soon.
    </div>
  );
}
